# ifupdown

## Usage

```
git clone https://github.com/iiAku/ifupdown
chmod a+x ifupdown.sh
./ifupdown.sh (ifup|ifdown) interfaceName until
eg: ./ifupdown.sh ifup eth0 5 will bring up eth0:1,eth0:2,eth0:3 
eg: ./ifupdown.sh ifdown eth0 5 will bring down eth0:1,eth0:2,eth0:3
```
